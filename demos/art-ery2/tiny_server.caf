import &ArtStandardLib, &ArtEry

&ArtConfig.configure()

pipelineRegistry = new PipelineRegistry

class Status extends Pipeline
  @registry pipelineRegistry
  @requestTypes get: response: data: :string
  @publicHandlers get: -> :up

&ArtEry/Server.start {} pipelineRegistry, verbose: true