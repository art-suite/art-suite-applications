import &ArtStandardLib

webpack:
  common:
    mode:     :development
    devtool:  :source-map

  targets:
    Demos: {}
    HelloWorld: {}

package: -> merge
  &package
  scripts:
    start:
      """
        nn -s --watch&
        cafSourceMaps=true webpack serve --hot --progress --open&
        wait
