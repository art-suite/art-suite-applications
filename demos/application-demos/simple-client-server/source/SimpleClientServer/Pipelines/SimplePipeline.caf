import &ArtStandardLib, &ArtEry

class SimplePipeline extends Pipeline

  @publicRequestTypes :get

  @handler
    get: ({key})-> "" The #{if isBrowser then :client else :server} received your request for: #{key}.
