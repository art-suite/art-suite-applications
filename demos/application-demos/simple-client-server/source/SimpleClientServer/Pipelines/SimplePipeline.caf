import &ArtStandardLib, &ArtEry

class SimplePipeline extends Pipeline

  @publicHandlers
    get: ({type, key, location}) ->
      "" Request '#{type}:#{key}' processed by #{if isBrowser then :client else :server}.
