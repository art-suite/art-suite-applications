import &StandardImport

class WidgetLib
  @drawButton: ({hover, disabled, gridSize}, {drawPadding}) ->
    padding: drawPadding
    radius: gridSize / 2
    fill:
      if hover && !disabled
        AppPalette.primary
      else
        AppPalette.primary
        .withAlpha 0