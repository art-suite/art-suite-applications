import &ArtStandardLib

&ToolLib.createPipelineCommand
  description: "DynamoDb: show initialize params"
  action: (pipeline, {pipelines}) ->
    pipeline?.getInitializeParams originatedOnServer: true
    ? Promise.deepAll merge object pipeline in pipelines with-key pipeline.pipelineName
      pipeline.getInitializeParams? originatedOnServer: true
